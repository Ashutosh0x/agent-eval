node_modules/
dist/
build/
.turbo/
*.tsbuildinfo

# Secrets and local config
.env
.env.local
.env.*.local

# Local state
*.log
coverage/
.DS_Store
spec.tmp.json

# Vercel CLI link state: contains project and org identifiers.
.vercel

# Downloaded toolchain (opa binary, ~96MB). Fetched by scripts, never committed.
.tools/

# opa build drops a dot-file manifest; the script copies it to manifest.json.
packages/server/policy-bundle/.manifest
